spring:
  jackson:
    property-naming-strategy: SNAKE_CASE

  datasource:
    url: jdbc:h2:mem:verby_test;MODE=MYSQL

  redis:
    host: localhost
    port: 9341

  session:
    store-type: redis

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        show_sql: true
        format_sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: none

  sql:
    init:
      schema-locations: classpath:sql/schema-h2.sql
      data-locations: classpath:sql/init-account.sql
      mode: always

cloud:
  aws:
    region:
      static: ap-northeast-2
      auto: false
    s3:
      bucket: verby-deploy
    stack:
      auto: false

logging:
  level:
    root: INFO
    com.amazonaws.util.EC2MetadataUtils: error

static:
  paths:
    song:
      image: static/song/images