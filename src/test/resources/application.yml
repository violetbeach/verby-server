spring:
  jackson:
    property-naming-strategy: SNAKE_CASE

  datasource:
    url: jdbc:h2:mem:verby_test;

  redis:
    host: localhost
    port: 9341

  session:
    store-type: redis

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        show_sql: true
        format_sql: true
    defer-datasource-initialization: true

  sql:
    init:
      data-locations: classpath:sql/init-role.sql, classpath:sql/init-account.sql

cloud:
  aws:
    region:
      static: ap-northeast-2
      auto: false
    s3:
      bucket: verby-deploy
    stack:
      auto: false

logging:
  level:
    root: INFO
    com.amazonaws.util.EC2MetadataUtils: error

static:
  paths:
    song:
      image: static/song/images